import { makeStyles } from '@material-ui/styles'

const DrawerStyles = makeStyles({
  root: {
    color: 'white',
    width: 300
  },
  drawerPaper: {
    width: 300
  }
})
export default DrawerStyles
